name: car_wash
description: تطبيق غسيل سيارات - عميل ومقدم خدمة
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^8.1.5
  equatable: ^2.0.5
  dio: ^5.4.2
  flutter_screenutil: ^5.9.0
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0

  # # Optional: لو هتستخدم خرائط أو مواقع
  # geolocator: ^10.1.0
  # google_maps_flutter: ^2.5.3

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
